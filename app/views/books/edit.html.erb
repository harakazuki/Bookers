<h1>Editing Book</h1>
<%= form_with model: @book do |f| %>

  <div>Title</div>
  <%= f.text_field :title %>
  
  <div>Body</div>
  <%= f.text_area :body %>
  
  <%= f.submit 'Update Book' %>
  
  <a href="<%= book_path(@book) %>">Show</a>
  <%= link_to 'Back', articles_path %>
<% end %>